//
// Copyright (c) Vatsal Manot
//

import Swift

struct CSVHeader {
    let index: Int
    let key: String

    init(index: Int, key: String) {
        self.index = index
        self.key = key
    }
}
